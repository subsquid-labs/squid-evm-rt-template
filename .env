DB_NAME=postgres
DB_PORT=23798
GQL_PORT=4350

# RPC is only used for explicit calls (e.g. contract state inquiries)
# and is not involved in ingestion
RPC_ETH_HTTP=<my_rpc_url>

# Uncommenting this line enables the debug mode
# More info at https://docs.subsquid.io/basics/logging/
#SQD_DEBUG=*
